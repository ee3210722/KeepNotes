import { createContext } from 'react';

// noteContext will holds all the state related to notes
const noteContext = createContext();

export default noteContext;

// Here, we used context api. but what is context api here?